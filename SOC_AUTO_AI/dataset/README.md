# Dataset

`raw/sample.jsonl` is a small labeled development set. The Wazuh and Suricata files represent normalized source events. All records use the input schema in the repository root. Run `python scripts/generate_sample_data.py` to create a larger local sample.
